#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int main(int argc, char **argv) 
{
    int secs;
/*
    if (argc != 2) {
        exit(0);
    }
 */   
    // sleep the program as many seconds as you wish
    //secs = atoi(argv[1]);
    sleep(15);

    exit(0);
}
